import { iCourse } from "../interface/course";
import { iPageClipData } from "../interface/pageClipData";

export const isSeatOpen = (
  CRN: number,
  data: iPageClipData
): iCourse | null => {
  return null;
};
